{
  "name": "ai-video-detector-static-site",
  "version": "0.1.0",
  "private": true,
  "engines": {"node": ">=20"},
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "preview": "npx serve dist -l 4173"
  },
  "devDependencies": {"serve": "latest"}
}
